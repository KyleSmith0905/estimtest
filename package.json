{
  "name": "estimtest",
  "description": "",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap --no-ci",
    "build": "lerna run build",
    "dev": "cd packages/estimtest-core && npm run build",
    "test": "lerna run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "author": "KyleSmith0905",
  "license": "ISC",
  "dependencies": {
    "find-up": "^6.3.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "lerna": "^6.5.1",
    "typescript": "^4.9.5"
  }
}
