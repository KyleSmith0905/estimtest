---
description: The port of the web server.
sidebar_position: 3
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Web Port
The web port is the port of your server. Opening up this port on the browser should show your website. This defaults to 3000, which is a common port for web development.

## Example
In this example we are changing our port to 4200.

<Tabs
  defaultValue='vanilla'
  groupId='framework'
  values={[
    { label: 'CLI', value: 'cli', },
    { label: 'Vanilla HTML', value: 'vanilla', },
    { label: 'Angular', value: 'angular', },
    { label: 'React', value: 'react', },
    { label: 'Vue', value: 'vue', },
  ]}
>
  <TabItem value="cli">

```js title="/estimtest.config.js"
/** @type {import('estimtest-utils/config').EstimtestConfig} */
const config = {
  estimtestPort: 4200,
}

export default config;
```
  </TabItem>
  <TabItem value="vanilla">

The `web-port` property is not supported by Estimtest component libraries. It is only capable of accessing web pages in which the Estimtest component is located.
  </TabItem>
  <TabItem value="angular">

The `web-port` property is not supported by Estimtest component libraries. It is only capable of accessing web pages in which the Estimtest component is located.
  </TabItem>
  <TabItem value="react">

The `web-port` property is not supported by Estimtest component libraries. It is only capable of accessing web pages in which the Estimtest component is located.
  </TabItem>
  <TabItem value="vue">

The `web-port` property is not supported by Estimtest component libraries. It is only capable of accessing web pages in which the Estimtest component is located.
  </TabItem>
</Tabs>